node_modules/
*.log
results.tsv
.autotune/
.DS_Store
# Generated viewer manifests (rebuild with apa-manifest)
**/manifest.json
# Real patent matters are confidential - never commit a matter outside examples/
/matters/
*.local
.apa/
# Installer bundle is regenerated by prepack/bundle-skills from the repo-root skills/
packages/apa-skills/skills/
# Per-host generated skills (build artifact; canonical Claude output lives in skills/)
/dist/
# apa-eval run records (periodic CI eval store)
/.eval-runs/
